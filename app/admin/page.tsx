import { requireAdmin } from "@/lib/auth";
import { createServerSupabaseClient } from "@/lib/supabase/server";
import { createCatalogService, createMemberPlan } from "@/app/admin/actions";

export const dynamic = "force-dynamic";
export const metadata = { title: "UniPlug administration" };

export default async function AdminPage() {
  await requireAdmin();
  const supabase = await createServerSupabaseClient();
  const [{ data: services }, { data: plans }] = supabase ? await Promise.all([
    supabase.from("uniplug_catalog_services").select("id,name,slug,category_slug,is_active").order("name"),
    supabase.from("uniplug_member_plans").select("id,plan_name,plan_code,price_kes,billing_cycle,is_active,service:uniplug_catalog_services(name)").order("created_at", { ascending: false })
  ]) : [{ data: [] }, { data: [] }];

  return <section className="section shell page-top"><div className="page-heading"><p className="eyebrow">UniPlug operations</p><h1>Catalog and private member plans</h1><p>Public service content is managed separately from authenticated pricing.</p></div><div className="admin-grid"><section className="panel"><h2>Add catalog service</h2><form action={createCatalogService} className="admin-form"><input name="name" placeholder="Service name" required /><input name="slug" placeholder="service-slug" /><select name="category" defaultValue="productivity"><option value="streaming">Streaming</option><option value="music">Music</option><option value="creative">Creative</option><option value="ai">AI tools</option><option value="productivity">Productivity</option><option value="cloud">Cloud</option><option value="security">Security</option><option value="gaming">Gaming</option><option value="learning">Learning</option></select><input name="shortDescription" placeholder="Short description" required /><textarea name="description" placeholder="Full service description" required /><div className="form-row"><input name="logoText" placeholder="Logo text" /><input name="accentColor" type="color" defaultValue="#6957ff" /></div><input name="fulfillmentLabel" placeholder="Managed access" /><input name="activationWindow" placeholder="Activation window" /><textarea name="replacementSummary" placeholder="Replacement-policy summary" /><button className="button button-dark">Create service</button></form></section><section className="panel"><h2>Add private plan</h2><form action={createMemberPlan} className="admin-form"><select name="serviceId" required><option value="">Choose service</option>{(services || []).map((service) => <option key={service.id} value={service.id}>{service.name}</option>)}</select><input name="planName" placeholder="Plan name" required /><input name="planCode" placeholder="plan-code" /><div className="form-row"><input name="priceKes" type="number" min="1" placeholder="Price in KSh" required /><input name="compareAtKes" type="number" min="1" placeholder="Compare-at price" /></div><select name="billingCycle"><option value="monthly">Monthly</option><option value="quarterly">Quarterly</option><option value="yearly">Yearly</option></select><button className="button button-dark">Create member plan</button></form></section></div><div className="admin-grid lists"><section className="panel"><h2>Catalog services</h2><div className="admin-list">{(services || []).map((service) => <div key={service.id}><div><strong>{service.name}</strong><span>/{service.slug} · {service.category_slug}</span></div><span className="status-pill">{service.is_active ? "active" : "hidden"}</span></div>)}</div></section><section className="panel"><h2>Member plans</h2><div className="admin-list">{(plans || []).map((plan) => { const service = plan.service as unknown as { name?: string } | null; return <div key={plan.id}><div><strong>{service?.name || "Service"} — {plan.plan_name}</strong><span>{plan.plan_code} · {plan.billing_cycle}</span></div><strong>KSh {Number(plan.price_kes).toLocaleString("en-KE")}</strong></div>; })}</div></section></div></section>;
}
